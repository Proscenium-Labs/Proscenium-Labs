Co-pilot prompts go here.

Use a slash command to run a reusable prompt.
The command will be the file name; in this case it was /my-prompts.
In other IDEs such as Visual Studio you would reference the reusable prompt by first typing #prompt: then the file name.
